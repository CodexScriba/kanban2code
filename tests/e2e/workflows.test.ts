/**
 * Placeholder for VS Code test-electron flows.
 * These are marked skipped to keep unit test runs fast; run with a VS Code
 * integration harness when exercising the extension end-to-end.
 */
describe.skip('e2e workflows (VS Code)', () => {
  it('scaffolds a workspace and opens the board', async () => {
    // Use @vscode/test-electron here to launch the extension host and drive UI.
  });

  it('creates tasks from sidebar and board', async () => {
    // TODO: Implement once fixtures are wired into the test-electron harness.
  });

  it('moves tasks across stages and archives them', async () => {
    // TODO
  });

  it('copies XML context from a task', async () => {
    // TODO
  });
});

