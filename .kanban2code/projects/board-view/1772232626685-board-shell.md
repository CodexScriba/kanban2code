---
stage: completed
tags:
  - ui
  - board
  - webview
contexts:
  - architecture.md
skills:
  - skill-vscode
agent: auditor
---

# Board-shell

## Goal

Implement the Kanban board UI shell as a static, styled webview panel in the editor area, using `kanbanboard-codex.html` as a design template. The board should open when the "Kanban" button is clicked in the sidebar.

## Definition of Done

- [x] `esbuild.mjs` updated to support multiple webview entry points (`webview` and `board`)
- [x] `KanbanPanel.ts` created to manage the singleton `WebviewPanel` in the editor area
- [x] `src/webview/ui/board.tsx` and `board.css` created by porting the mockup's DOM and styles
- [x] Sidebar "Kanban" button triggers the board opening via message passing to the host
- [x] `package.json` registered with the `kanban2code.openBoard` command
- [x] Board renders correctly in the Extension Development Host with hardcoded placeholder data

## Audit

- `esbuild.mjs`
- `package.json`
- `src/extension.ts`
- `src/webview/KanbanPanel.ts`
- `src/webview/SidebarProvider.ts`
- `src/webview/messaging.ts`
- `src/webview/ui/board.css`
- `src/webview/ui/board.tsx`
- `src/webview/ui/index.tsx`

## Files

- `src/webview/KanbanPanel.ts` — ← create: Singleton manager for the board webview
- `src/webview/ui/board.tsx` — ← create: Board UI entry point (vanilla DOM implementation)
- `src/webview/ui/board.css` — ← create: Stylesheet ported from `kanbanboard-codex.html`
- `src/webview/SidebarProvider.ts` — ← modify: Post message to host on Kanban button click
- `src/extension.ts` — ← modify: Handle sidebar messages and register the open command
- `esbuild.mjs` — ← modify: Add `board.tsx` entry point
- `package.json` — ← modify: Register command and activation events

## Refined Prompt

Objective: Implement the Kanban board UI shell as a managed webview panel, using the provided mockup as a design reference.

Implementation approach:
1. **Build Pipeline:** Update `esbuild.mjs` to use an object for `entryPoints`. Map `src/webview/ui/index.tsx` to `webview` and `src/webview/ui/board.tsx` to `board`. This ensures we get separate `board.js` and `board.css` outputs.
2. **Panel Management:** Create `src/webview/KanbanPanel.ts`. Implement a singleton class that wraps `vscode.WebviewPanel`. It should handle HTML generation with a proper CSP, nonces, and asset resolution for the `board` bundle.
3. **UI Implementation:** Create `src/webview/ui/board.tsx` and `board.css`. Port the HTML structure and CSS from `kanbanboard-codex.html` verbatim. Use vanilla DOM manipulation to mount the UI into `#app`.
4. **Wiring:** 
    - In `SidebarProvider.ts`, add a click listener to `#viewKanbanBtn` that posts a `show-kanban` message.
    - In `extension.ts`, listen for this message and call `KanbanPanel.createOrShow(context.extensionUri)`.
    - Register `kanban2code.openBoard` as a command that also triggers the panel.

Key decisions:
- **Named Bundles:** Using named entry points in esbuild allows the sidebar and board to remain decoupled and optimized.
- **Vanilla DOM:** Maintain consistency with the existing sidebar implementation by avoiding a React runtime for now.

## Context

### File Tree (scoped)

```
src/
├── extension.ts                      # ← modify (command registration)
└── webview/
    ├── KanbanPanel.ts                # ← create (panel lifecycle)
    ├── SidebarProvider.ts            # ← modify (message trigger)
    └── ui/
        ├── board.tsx                 # ← create (board DOM)
        ├── board.css                 # ← create (board styles)
        └── index.tsx                 # ← read-only reference
esbuild.mjs                           # ← modify (entry points)
package.json                          # ← modify (commands)
kanbanboard-codex.html                # ← read-only reference (template)
```

### Architecture Excerpts

> **Product Goal:** Kanban view: clear stage-based task board (inbox -> plan -> code -> audit -> completed).
> **Extension structure:** Extension host registers a WebviewViewProvider that serves a bundled React-ready webview with CSP and theme integration.

### Code Excerpts

`esbuild.mjs:17-28` — Current webview configuration:
```javascript
const webviewConfig = {
  entryPoints: ['src/webview/ui/index.tsx'],
  outdir: 'dist',
  // ...
  entryNames: 'webview',
```

`src/webview/SidebarProvider.ts:85-98` — Kanban button listener logic to be added:
```typescript
kanbanBtn.addEventListener('click', () => {
  vscodeApi?.postMessage({ type: 'ShowKanbanBoard' });
});
```

### Dependency Graph

- `SidebarProvider` (UI) → `extension.ts` (via `postMessage`)
- `extension.ts` → `KanbanPanel` (via `createOrShow`)
- `KanbanPanel` → `board.js` / `board.css` (via HTML template)

### Gotchas

- **CSP:** Ensure the board's CSP allows `https://fonts.googleapis.com` and `https://fonts.gstatic.com` as the mockup uses the Inter font.
- **Asset Names:** When switching esbuild to an object for `entryPoints`, ensure `entryNames` is set to `[name]` so the sidebar remains `webview.js`.

### Scope Boundaries

- DO NOT implement task persistence or file system reading in this task.
- DO NOT implement drag-and-drop functionality.
- DO NOT implement the "OpenClaw" connection flow.

---

## Review

**Rating: 8/10**

**Verdict: ACCEPTED**

### Summary
The board shell implementation meets the defined scope and DoD: command wiring, sidebar-triggered board opening, multi-entry webview bundling, and static placeholder rendering are all in place. Build and typecheck both pass.

### Findings

#### Blockers
- [ ] None.

#### High Priority
- [ ] None.

#### Medium Priority
- [ ] CSP hardening opportunity: board HTML currently permits inline scripts and uses many inline event handlers. Prefer `addEventListener` wiring so `'unsafe-inline'` can be removed from `script-src` - `src/webview/KanbanPanel.ts:75`, `src/webview/ui/board.tsx:10`

#### Low Priority / Nits
- [ ] None.

### Test Assessment
- Coverage: Needs improvement
- Missing tests: Message path coverage for `ShowKanbanBoard` (`index.tsx` -> `SidebarProvider` -> command), command registration smoke, and `KanbanPanel` singleton behavior

### What's Good
- Clean host/webview separation with typed message validation (`ShowKanbanBoard` added to messaging contract).
- Build pipeline update is correct and keeps deterministic output names (`webview.*` and `board.*`).
- Command and sidebar button both route to the same board-open path, reducing drift.

### Recommendations
- Follow up with a security-hardening pass to replace inline handlers in `board.tsx` and tighten CSP accordingly.
